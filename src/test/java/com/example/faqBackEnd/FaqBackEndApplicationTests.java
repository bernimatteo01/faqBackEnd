package com.example.faqBackEnd;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Test di avvio dell'applicazione Spring Boot.
 * Verifica che il contesto dell'applicazione venga caricato correttamente
 * senza errori di configurazione.
 */
@SpringBootTest
class FaqBackEndApplicationTests {

    /**
     * Test base generato automaticamente da Spring Boot.
     * Se il contesto si carica correttamente, il test passa.
     * In caso contrario (es. configurazione errata o bean mancanti), il test fallisce.
     */
    @Test
    void contextLoads() {
        // Test vuoto: serve solo a verificare il caricamento del contesto
    }

}